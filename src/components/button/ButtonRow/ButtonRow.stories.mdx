import { useState } from "react";
import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs";
import { ButtonRow } from "./ButtonRow";
import { Button } from "../Button";

<Meta title="Components/Buttons/ButtonRow" component={ButtonRow} />

# ButtonRow

Write something here.

<ArgsTable of={ButtonRow} />

### Basic

<Canvas>
    <Story name="basic">
        {() => {
            const [index, setIndex] = useState(0);
            return (
                <Row>
                    <Col span={24}>
                        <ButtonRow>
                            <Button active={index === 0} onClick={() => setIndex(0)}>
                                First
                            </Button>
                            <Button active={index === 1} onClick={() => setIndex(1)}>
                                Second
                            </Button>
                            <Button active={index === 2} onClick={() => setIndex(2)}>
                                Third
                            </Button>
                        </ButtonRow>
                    </Col>
                </Row>
            );
        }}
    </Story>
</Canvas>
