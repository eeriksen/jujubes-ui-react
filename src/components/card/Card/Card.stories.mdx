import { useState } from "react";
import { Meta, Story, Preview, ArgsTable } from '@storybook/addon-docs/blocks'
import { Card } from './Card'
import { CardContent } from "../CardContent"
import { CardTitle } from "../CardTitle"
import { CardUserHead } from "../CardUserHead"
import { Badge } from "../../notify/Badge"
import { userPictureUrl } from "../../../../.storybook/constants"

<Meta title="Components/Cards/Card" component={Card} />

# Card

A `<Card />` is used to wrap sections of content on a `<Page />`.
The `<Card/>` component is should contain children of other Card-components.

<ArgsTable of={Card} />
