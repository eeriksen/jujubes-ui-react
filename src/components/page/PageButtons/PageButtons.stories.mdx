import { useState } from "react";
import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs/blocks";
import { PageButtons } from "./PageButtons";
import { PageButton } from "./PageButton";

<Meta title="Components/Page/PageButtons" component={PageButtons} />

# PageButtons

The `<PageButtons />` renders big navigational buttons at the top of a page.
Use these buttons to split up content into different pages within a context.
These should only be placed on the top of the page, just below the `<PageHeader />`.

<ArgsTable of={PageButtons} />

### Basic

<Canvas>
    <Story name="basic">
        {() => {
            const [activeIndex, setActiveIndex] = useState(1);
            return (
                <PageButtons>
                    <PageButton active={activeIndex === 1} onClick={() => setActiveIndex(1)} icon="edit" label="Details" link="/details" />
                    <PageButton active={activeIndex === 2} onClick={() => setActiveIndex(2)} icon="devices" label="Devices" count={29} link="/devices" />
                    <PageButton active={activeIndex === 3} onClick={() => setActiveIndex(3)} icon="palette" label="Theme" link="/theme" />
                </PageButtons>
            );
        }}
    </Story>
</Canvas>
