import { useState } from "react";
import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs";
import { Breadcrumb } from "./Breadcrumb";

<Meta title="Components/Navigation/Breadcrumb" component={Breadcrumb} />

# Breadcrumb

Place a Breadcrumb on a page to add a crumb to the trail.

<ArgsTable of={Breadcrumb} />

### Basic

<Canvas>
    <Story name="basic">
        <Breadcrumb>Something here</Breadcrumb>
    </Story>
</Canvas>
