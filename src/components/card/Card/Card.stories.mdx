import { useState } from "react";
import { Meta, Canvas, Story, Preview, ArgsTable } from "@storybook/addon-docs";
import { Card } from "./Card";
import { CardContent } from "../CardContent";
import { CardTitle } from "../CardTitle";
import { Text } from "../../typography/Text";
import { Button } from "../../button/Button";

<Meta title="Components/Cards/Card" component={Card} />

# Card

A `<Card />` is used to wrap sections of content on a `<Page />`.
The `<Card/>` component is should contain children of other Card-components.

<ArgsTable of={Card} />

### Clickable

<Canvas>
    <Story name="clickable">
        <Card onClick={() => console.log("CLICKED")}>
            <CardContent>
                Lorem ipsum dolor sit amet, mel aliquip consetetur ea, postulant salutandi et ius.
                Quas semper dissentiunt ad mel, in putent minimum ius, ne doctus audiam menandri
                mel. In amet utroque facilisi quo, mei ad mutat graeci evertitur, natum fastidii no
                mel. Etiam praesent moderatius an vis, vim velit tollit cu.
            </CardContent>
        </Card>
    </Story>
</Canvas>
