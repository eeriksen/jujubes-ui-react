import { useState } from "react";
import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs";
import { AppLayout } from "../AppLayout";
import { AppNav } from "./AppNav";
import { ComponentWrapper } from "../ComponentWrapper";

<Meta title="Components/Layout/AppNav" component={AppNav} />

# AppNav

The `<AppNav />` component renders a navigation sidebar that is always visible on large screens
and togglable from the `<AppBar />` component on mobile devices.

<ArgsTable of={AppNav} />

### Basic

<Canvas>
    <Story name="basic">
        <ComponentWrapper>
            <AppLayout>
                <AppNav></AppNav>
            </AppLayout>
        </ComponentWrapper>
    </Story>
</Canvas>
