import { useState } from "react";
import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs";
import { PageHero } from "./PageHero";
import { Image } from "../../media/Image";
import { SAMPLE_USER } from "../../../../.storybook/constants";

<Meta title="Components/Page/PageHero" component={PageHero} />

# PageHero

Write something here.

<ArgsTable of={PageHero} />

### Basic

<Canvas>
    <Story name="basic">
        <PageHero
            title={SAMPLE_USER.name}
            subtitle={SAMPLE_USER.email}
            image={
                <Image
                    src={SAMPLE_USER.pictureUrl}
                    scale="fill"
                    placeholder={<Icon name="user" color="primary" />}
                    mod="shine"
                />
            }
        >
            <LogoSymbol height={40} />
        </PageHero>
    </Story>
</Canvas>
