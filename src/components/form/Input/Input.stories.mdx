import { useState } from "react";
import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs";
import { Form } from "../Form";
import { FormItem } from "../FormItem";
import { Input } from "./Input";
import { Text } from "../../typography/Text";

<Meta title="Components/Forms/Input" component={Input} />

# Input

The `<Input />` component is a very flexible text input component.

<ArgsTable of={Input} />

### Basic

<Canvas>
    <Story name="basic">
        {() => {
            const [value, setValue] = useState(null);
            return (
                <Form>
                    <FormItem>
                        <Input value={value} onChange={setValue} placeholder="Basic input" />
                    </FormItem>
                </Form>
            );
        }}
    </Story>
</Canvas>

### Size

<Canvas>
    <Story name="size">
        {() => {
            const [value, setValue] = useState(null);
            return (
                <Form>
                    <FormItem>
                        <Input
                            size="small"
                            value={value}
                            onChange={setValue}
                            placeholder="Small input"
                        />
                    </FormItem>
                    <FormItem>
                        <Input value={value} onChange={setValue} placeholder="Regular input" />
                    </FormItem>
                    <FormItem>
                        <Input
                            size="big"
                            value={value}
                            onChange={setValue}
                            placeholder="Big input"
                        />
                    </FormItem>
                </Form>
            );
        }}
    </Story>
</Canvas>

### Icon

<Canvas>
    <Story name="icon">
        {() => {
            const [value, setValue] = useState(null);
            return (
                <Form>
                    <FormItem>
                        <Input
                            size="small"
                            icon="link"
                            value={value}
                            onChange={setValue}
                            placeholder="Small input with icon"
                        />
                    </FormItem>
                    <FormItem>
                        <Input
                            icon="link"
                            value={value}
                            onChange={setValue}
                            placeholder="Regular input with icon"
                        />
                    </FormItem>
                    <FormItem>
                        <Input
                            size="big"
                            icon="link"
                            value={value}
                            onChange={setValue}
                            placeholder="Big input with icon"
                        />
                    </FormItem>
                </Form>
            );
        }}
    </Story>
</Canvas>

### Error

<Canvas>
    <Story name="error">
        {() => {
            const [value, setValue] = useState(null);
            return (
                <Form>
                    <FormItem error>
                        <Input
                            type="password"
                            icon="lock"
                            value={value}
                            onChange={setValue}
                            placeholder="Input with error"
                        />
                    </FormItem>
                </Form>
            );
        }}
    </Story>
</Canvas>

### Prepend

<Canvas>
    <Story name="prepend">
        {() => {
            const [value, setValue] = useState(null);
            return (
                <Form>
                    <FormItem>
                        <Input
                            value={value}
                            onChange={setValue}
                            placeholder="Input with prepend"
                            prepend={<Text opacity={60}>http://</Text>}
                        />
                    </FormItem>
                </Form>
            );
        }}
    </Story>
</Canvas>

### Append

<Canvas>
    <Story name="append">
        {() => {
            const [value, setValue] = useState(null);
            return (
                <Form>
                    <FormItem>
                        <Input
                            value={value}
                            onChange={setValue}
                            placeholder="Input with append"
                            append={<Text opacity={60}>.com</Text>}
                        />
                    </FormItem>
                </Form>
            );
        }}
    </Story>
</Canvas>

### Disabled

<Canvas>
    <Story name="disabled">
        {() => {
            const [value, setValue] = useState(null);
            return (
                <Form>
                    <FormItem>
                        <Input
                            value={value}
                            onChange={setValue}
                            placeholder="Disabled input"
                            disabled
                        />
                    </FormItem>
                </Form>
            );
        }}
    </Story>
</Canvas>
